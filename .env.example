# Local development only. Compose uses DB_*; local npm commands use DATABASE_URL.
DATABASE_URL=postgres://jobqueue:jobqueue@127.0.0.1:15432/jobqueue
DB_USER=jobqueue
DB_PASS=jobqueue
DB_NAME=jobqueue
PORT=8080
